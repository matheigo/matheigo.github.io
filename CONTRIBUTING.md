# 貢献のしかた

## 一番ありがたい報告

**「授業で先生が実際にこう言っていた」**という報告です。辞書に載っている訳語より、
教室で耳にした言い回しのほうが価値があります。科目名（Calc I、Precalc など）と、
できれば前後の文脈を添えてください。

## 報告できること

- **誤訳・不自然な英語** — 「米国ではそうは言わない」
- **抜けている語** — 授業で困った語
- **対応の誤り** — `mapping: exact` になっているが実は 1 対 1 ではない、など
- **慣習差** — 日本と米国で書き方が違って戸惑ったところ

issue テンプレートを用意しています。

## PR を送る場合

1. `data/<collection>/<id>.json` を 1 ファイル 1 エントリで追加・編集する
2. `docs/STYLE.md` の直訳禁止リストと 10 原則を確認する
3. 定義と例文は自分の言葉で書く。他の辞書・教科書・サイトから写さない（データは CC0 なので、
   CC BY-SA や商用辞書の文言が混ざるとライセンスが破綻します）
4. 出典を 1 件以上付ける。付けられなければ `confidence: draft` のままにする
5. `pnpm validate && pnpm spell && pnpm test` が通ることを確認する

`confidence` を `verified` に上げるのは監査を通ったときだけです。自分で追加した語を自分で
`verified` にはしないでください。
